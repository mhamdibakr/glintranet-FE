import { EntityDepartment } from './entity-department.model';

describe('EntityDepartment', () => {
  it('should create an instance', () => {
    expect(new EntityDepartment()).toBeTruthy();
  });
});
