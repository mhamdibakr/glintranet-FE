import { GlobalExperience } from './global-experience.model';

describe('GlobalExperience', () => {
  it('should create an instance', () => {
    expect(new GlobalExperience()).toBeTruthy();
  });
});
